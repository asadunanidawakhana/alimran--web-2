import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alimran.com',
  appName: 'al imran tense group / tenses',
  webDir: 'out'
};

export default config;
